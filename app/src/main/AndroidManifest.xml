<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dntrindade.weightlosstracker">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeightLossTracker">
        <activity android:name=".CorrerActivity"></activity>
        <activity android:name=".AlimentacaoActivity" />
        <activity android:name=".correr" />
        <activity android:name=".TmbActivity" />
        <activity
            android:name=".ListCalcActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".ImcActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>